import React from 'react';
import "./styles.scss";

const Header = props =>{
    return(
        <header className="header">
            <h1 className="logo"><i>WHISKEY SELECTION</i></h1>
        </header>
    );
};

export default Header;